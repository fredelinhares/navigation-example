<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/featureaNavGraph">

    <include app:graph="@navigation/featurea_nav_graph" />

    <include app:graph="@navigation/featureb_nav_graph" />

    <include app:graph="@navigation/featurec_nav_graph" />

    <action
        android:id="@+id/action_global_featureAFragment"
        app:destination="@id/featureaNavGraph" />

    <action
        android:id="@+id/action_global_featureBFragment"
        app:destination="@id/featurebNavGraph" />

    <action
        android:id="@+id/action_global_featureCFragment"
        app:destination="@id/featurecNavGraph" />

</navigation>