pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Esta linha será removida em breve, pois o JCenter será desativado
    }
}
rootProject.name = "navigation-example"
include ':app'
include ':featurea'
include ':featureb'
include ':featurec'
